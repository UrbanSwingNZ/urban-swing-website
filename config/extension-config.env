# Firebase Extension: Trigger Email from Firestore
# Configuration for firestore-send-email extension

# Firestore database to use
FIRESTORE_INSTANCE_ID=(default)

# Firestore database location
LOCATION=us-central1

# Authentication type (use NONE for username/password via SMTP URI)
SMTP_CONNECTION_URI=smtps://dance@urbanswing.co.nz:mckscfnpcqyqulub@smtp.gmail.com:465

# Email collection in Firestore
MAIL_COLLECTION=mail

# Default FROM email address
DEFAULT_FROM=dance@urbanswing.co.nz

# Default REPLY-TO email address (optional)
DEFAULT_REPLY_TO=dance@urbanswing.co.nz
